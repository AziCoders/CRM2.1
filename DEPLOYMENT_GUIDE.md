# Полная инструкция по развёртыванию бота на сервере 24/7

## Оглавление
1. [Подготовка к подключению](#1-подготовка-к-подключению)
2. [Подключение к серверу через Putty](#2-подключение-к-серверу-через-putty)
3. [Установка необходимого ПО на сервере](#3-установка-необходимого-по-на-сервере)
4. [Клонирование проекта с GitHub](#4-клонирование-проекта-с-github)
5. [Настройка окружения](#5-настройка-окружения)
6. [Настройка автозапуска через systemd](#6-настройка-автозапуска-через-systemd)
7. [Управление ботом](#7-управление-ботом)
8. [Мониторинг и логи](#8-мониторинг-и-логи)
9. [Решение проблем](#9-решение-проблем)

---

## 1. Подготовка к подключению

### Что понадобится:
- IP-адрес или домен вашего сервера
- Имя пользователя для SSH (обычно `root` или ваш пользователь)
- Пароль или SSH-ключ для доступа
- Ссылка на GitHub репозиторий с ботом

---

## 2. Подключение к серверу через Putty

### Шаг 1: Открытие Putty
1. Запустите программу Putty
2. В поле **Host Name (or IP address)** введите IP-адрес вашего сервера или домен
3. Убедитесь, что **Port** установлен на `22`
4. В разделе **Connection type** выберите **SSH**
5. (Опционально) Сохраните сессию: введите название в **Saved Sessions** и нажмите **Save**

### Шаг 2: Настройка SSH
1. Перейдите в раздел **Connection → Data**
2. В поле **Auto-login username** введите имя пользователя (например, `root`)
3. Вернитесь в раздел **Session** и нажмите **Open**

### Шаг 3: Первое подключение
1. При первом подключении появится предупреждение о ключе сервера - нажмите **Yes**
2. Введите пароль (при вводе пароль не отображается - это нормально)
3. Нажмите **Enter**

### Шаг 4: Проверка подключения
После успешного подключения вы должны увидеть командную строку Linux, например:
```bash
root@your-server:~#
```

---

## 3. Установка необходимого ПО на сервере

### 3.1 Обновление системы
```bash
# Для Ubuntu/Debian
sudo apt update && sudo apt upgrade -y

# Для CentOS/RHEL
sudo yum update -y
```

### 3.2 Установка Python 3 и pip
```bash
# Для Ubuntu/Debian
sudo apt install python3 python3-pip python3-venv git -y

# Для CentOS/RHEL
sudo yum install python3 python3-pip git -y

# Проверка версии Python
python3 --version
# Должно быть Python 3.8 или выше
```

### 3.3 Установка systemd (обычно уже установлен)
```bash
systemctl --version
# Если команда работает, systemd уже установлен
```

---

## 4. Клонирование проекта с GitHub

### Шаг 1: Создание рабочей директории
```bash
# Создаём директорию для проектов (если её ещё нет)
mkdir -p ~/projects
cd ~/projects

# Или создайте директорию в другом месте по вашему усмотрению
# Например: /opt/bot или /home/yourusername/bot
```

### Шаг 2: Клонирование репозитория
```bash
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git crm-bot

# Перейдите в директорию проекта
cd crm-bot
```

### Шаг 3: Проверка содержимого
```bash
ls -la
# Вы должны увидеть структуру проекта: bot/, data/, requirements_bot.txt и т.д.
```

---

## 5. Настройка окружения

### 5.1 Создание виртуального окружения Python
```bash
# Создаём виртуальное окружение
python3 -m venv venv

# Активируем виртуальное окружение
source venv/bin/activate

# После активации в начале строки появится (venv)
```

### 5.2 Установка зависимостей
```bash
# Обновляем pip
pip install --upgrade pip

# Устанавливаем зависимости из requirements_bot.txt
pip install -r requirements_bot.txt

# Проверяем установку
pip list
```

### 5.3 Создание файла .env
```bash
# Создаём файл .env в корне проекта
nano .env
```

Вставьте следующее содержимое (замените значения на ваши):
```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
OWNER_ID=ваш_telegram_user_id
NOTION_API_KEY=ваш_notion_api_key_если_используется
ROLES_FILE=/home/yourusername/projects/crm-bot/roles.json
```

**Как узнать OWNER_ID:**
1. Напишите боту @userinfobot в Telegram
2. Он выдаст ваш ID - это и есть OWNER_ID

**Сохранение файла в nano:**
- Нажмите `Ctrl + O` (Сохранить)
- Нажмите `Enter` (Подтвердить имя файла)
- Нажмите `Ctrl + X` (Выйти)

### 5.4 Проверка файла roles.json
```bash
# Проверьте, существует ли файл roles.json
ls -la roles.json

# Если его нет, создайте пустой файл
touch roles.json
echo '{}' > roles.json

# Убедитесь, что файл имеет правильные права
chmod 644 roles.json
```

### 5.5 Проверка структуры папок data
```bash
# Проверьте наличие папки data и её содержимого
ls -la data/

# Если папки нет, создайте базовую структуру
mkdir -p data
```

### 5.6 Тестовый запуск бота
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запустите бота для проверки
python3 bot/main.py
```

**Что должно произойти:**
- Бот должен запуститься без ошибок
- Вы увидите сообщение "Бот запущен и готов к работе"
- Если всё хорошо, остановите бота: нажмите `Ctrl + C`

**Если есть ошибки:**
- Проверьте правильность BOT_TOKEN в файле .env
- Проверьте правильность OWNER_ID
- Убедитесь, что все зависимости установлены

---

## 6. Настройка автозапуска через systemd

Systemd автоматически запустит бота при перезагрузке сервера и перезапустит его при ошибках.

### 6.1 Определение пути к Python
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Узнайте полный путь к Python в виртуальном окружении
which python
# Скопируйте этот путь (например: /home/yourusername/projects/crm-bot/venv/bin/python)

# Узнайте полный путь к директории проекта
pwd
# Скопируйте этот путь (например: /home/yourusername/projects/crm-bot)
```

### 6.2 Создание systemd service файла
```bash
# Создаём файл сервиса
sudo nano /etc/systemd/system/crm-bot.service
```

Вставьте следующее содержимое (замените пути на ваши):

```ini
[Unit]
Description=CRM Telegram Bot - 24/7 Service
After=network.target

[Service]
Type=simple
User=yourusername
Group=yourusername
WorkingDirectory=/home/yourusername/projects/crm-bot
Environment="PATH=/home/yourusername/projects/crm-bot/venv/bin"
ExecStart=/home/yourusername/projects/crm-bot/venv/bin/python /home/yourusername/projects/crm-bot/bot/main.py
Restart=always
RestartSec=10
StandardOutput=append:/home/yourusername/projects/crm-bot/logs/bot.log
StandardError=append:/home/yourusername/projects/crm-bot/logs/bot_error.log

# Защита от перезагрузки
StartLimitInterval=0

[Install]
WantedBy=multi-user.target
```

**Важно заменить:**
- `yourusername` - на ваше имя пользователя (можно узнать командой `whoami`)
- Все пути `/home/yourusername/projects/crm-bot` - на реальный путь к проекту
- Если у вас другой путь к Python, замените его

**Сохранение файла:**
- `Ctrl + O`, затем `Enter`
- `Ctrl + X`

### 6.3 Создание папки для логов
```bash
# Создаём папку для логов (если её нет)
mkdir -p ~/projects/crm-bot/logs

# Или если используете другой путь:
mkdir -p /полный/путь/к/проекту/logs

# Устанавливаем права
chmod 755 ~/projects/crm-bot/logs
```

### 6.4 Перезагрузка systemd и запуск сервиса
```bash
# Перезагружаем конфигурацию systemd
sudo systemctl daemon-reload

# Включаем автозапуск при загрузке системы
sudo systemctl enable crm-bot.service

# Запускаем бота
sudo systemctl start crm-bot.service

# Проверяем статус
sudo systemctl status crm-bot.service
```

**Что должно быть в статусе:**
- `Active: active (running)` - бот работает
- `Main PID: XXXX` - процесс запущен

**Если статус показывает ошибки:**
```bash
# Посмотрите детальные логи
sudo journalctl -u crm-bot.service -n 50 --no-pager
```

---

## 7. Управление ботом

### Остановка бота
```bash
sudo systemctl stop crm-bot.service
```

### Запуск бота
```bash
sudo systemctl start crm-bot.service
```

### Перезапуск бота
```bash
sudo systemctl restart crm-bot.service
```

### Проверка статуса
```bash
sudo systemctl status crm-bot.service
```

### Отключение автозапуска (если нужно)
```bash
sudo systemctl disable crm-bot.service
```

### Включение автозапуска обратно
```bash
sudo systemctl enable crm-bot.service
```

---

## 8. Мониторинг и логи

### Просмотр логов в реальном времени
```bash
# Логи systemd (рекомендуется)
sudo journalctl -u crm-bot.service -f

# Или логи из файлов (если настроены)
tail -f ~/projects/crm-bot/logs/bot.log
tail -f ~/projects/crm-bot/logs/bot_error.log
```

### Просмотр последних логов
```bash
# Последние 100 строк
sudo journalctl -u crm-bot.service -n 100

# Логи за сегодня
sudo journalctl -u crm-bot.service --since today

# Логи за последний час
sudo journalctl -u crm-bot.service --since "1 hour ago"
```

### Проверка, что бот работает
```bash
# Проверка процесса
ps aux | grep "bot/main.py"

# Должна быть строка с процессом python и bot/main.py
```

---

## 9. Решение проблем

### Проблема 1: Бот не запускается

**Симптомы:** `sudo systemctl status crm-bot.service` показывает `failed`

**Решение:**
```bash
# 1. Посмотрите детальные логи
sudo journalctl -u crm-bot.service -n 50 --no-pager

# 2. Проверьте, что все пути правильные в файле service
sudo nano /etc/systemd/system/crm-bot.service

# 3. Проверьте, что файл .env существует и содержит правильные данные
cat .env

# 4. Попробуйте запустить бота вручную для проверки
source venv/bin/activate
python3 bot/main.py
```

### Проблема 2: Бот падает с ошибками

**Симптомы:** Бот запускается, но затем падает

**Решение:**
```bash
# 1. Проверьте логи ошибок
sudo journalctl -u crm-bot.service -n 100 --no-pager | grep -i error

# 2. Проверьте файл roles.json на корректность
cat roles.json | python3 -m json.tool

# 3. Проверьте наличие всех необходимых файлов
ls -la roles.json
ls -la data/

# 4. Проверьте права доступа
ls -la
# Файлы должны быть читаемыми
```

### Проблема 3: Бот не перезапускается автоматически

**Симптомы:** После падения бот не запускается снова

**Решение:**
```bash
# 1. Проверьте настройки Restart в service файле
sudo nano /etc/systemd/system/crm-bot.service
# Убедитесь, что есть строки:
# Restart=always
# RestartSec=10

# 2. Перезагрузите конфигурацию
sudo systemctl daemon-reload

# 3. Перезапустите сервис
sudo systemctl restart crm-bot.service
```

### Проблема 4: Ошибка "Permission denied"

**Симптомы:** В логах видна ошибка доступа к файлам

**Решение:**
```bash
# 1. Проверьте права доступа к файлам
chmod 644 roles.json
chmod 755 data/

# 2. Проверьте, что пользователь в service файле правильный
whoami
# Сравните с User= в файле service

# 3. Проверьте права на директорию проекта
ls -ld ~/projects/crm-bot
```

### Проблема 5: Бот не запускается после перезагрузки сервера

**Симптомы:** После `sudo reboot` бот не стартует автоматически

**Решение:**
```bash
# 1. Проверьте, что сервис включён
sudo systemctl is-enabled crm-bot.service
# Должно вывести: enabled

# 2. Если нет, включите его
sudo systemctl enable crm-bot.service

# 3. Проверьте, что network.target доступен
systemctl status network.target
```

### Проблема 6: Ошибка с виртуальным окружением

**Симптомы:** Ошибка "No module named 'bot'" или похожие

**Решение:**
```bash
# 1. Убедитесь, что виртуальное окружение создано правильно
cd ~/projects/crm-bot
source venv/bin/activate
pip list
# Должны быть установлены все зависимости

# 2. Если что-то не так, пересоздайте виртуальное окружение
deactivate
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements_bot.txt

# 3. Обновите путь в service файле
sudo nano /etc/systemd/system/crm-bot.service
# Убедитесь, что пути к Python правильные
```

### Проблема 7: Бот работает, но не отвечает на сообщения

**Симптомы:** Статус показывает `running`, но бот не работает

**Решение:**
```bash
# 1. Проверьте логи на наличие ошибок
sudo journalctl -u crm-bot.service -f

# 2. Проверьте BOT_TOKEN
cat .env | grep BOT_TOKEN

# 3. Перезапустите бота
sudo systemctl restart crm-bot.service

# 4. Проверьте интернет-соединение сервера
ping -c 3 api.telegram.org
```

---

## 10. Дополнительные настройки

### Обновление бота с GitHub

Когда вы обновили код на GitHub и хотите обновить бота на сервере:

```bash
# 1. Остановите бота
sudo systemctl stop crm-bot.service

# 2. Перейдите в директорию проекта
cd ~/projects/crm-bot

# 3. Получите обновления
git pull origin main
# или git pull origin master (в зависимости от вашей ветки)

# 4. Обновите зависимости (если requirements_bot.txt изменился)
source venv/bin/activate
pip install -r requirements_bot.txt

# 5. Запустите бота обратно
sudo systemctl start crm-bot.service

# 6. Проверьте статус
sudo systemctl status crm-bot.service
```

### Настройка ротации логов

Чтобы логи не занимали слишком много места:

```bash
# Создайте конфигурацию для logrotate
sudo nano /etc/logrotate.d/crm-bot
```

Вставьте:
```
/home/yourusername/projects/crm-bot/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    notifempty
    missingok
}
```

### Мониторинг использования ресурсов

```bash
# Проверка использования CPU и памяти
top -p $(pgrep -f "bot/main.py")

# Или с помощью htop (если установлен)
htop -p $(pgrep -f "bot/main.py")
```

---

## 11. Проверочный чеклист

После завершения установки проверьте:

- [ ] Бот запущен: `sudo systemctl status crm-bot.service` показывает `active (running)`
- [ ] Автозапуск включен: `sudo systemctl is-enabled crm-bot.service` показывает `enabled`
- [ ] Бот отвечает на команду `/start` в Telegram
- [ ] Логи пишутся: `sudo journalctl -u crm-bot.service -n 20` показывает записи
- [ ] После перезагрузки сервера бот автоматически запускается
- [ ] При ошибке бот автоматически перезапускается (можно проверить, временно сломав .env файл)

---

## 12. Безопасность

### Рекомендации по безопасности:

1. **Не храните .env в Git:**
   ```bash
   # Убедитесь, что .env в .gitignore
   echo ".env" >> .gitignore
   ```

2. **Ограничьте доступ к файлам:**
   ```bash
   chmod 600 .env
   chmod 644 roles.json
   ```

3. **Используйте firewall:**
   ```bash
   # Откройте только необходимые порты
   sudo ufw allow 22/tcp  # SSH
   sudo ufw enable
   ```

4. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

---

## 13. Контакты и поддержка

Если у вас возникли проблемы, проверьте:

1. Логи: `sudo journalctl -u crm-bot.service -n 100`
2. Статус: `sudo systemctl status crm-bot.service`
3. Файл .env на наличие всех необходимых переменных
4. Права доступа к файлам и директориям

---

**Поздравляем! Ваш бот теперь работает 24/7 на сервере с автоматическим перезапуском при ошибках! 🎉**

